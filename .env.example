# Optional — enables cloud accounts + cross-device sync (Supabase Auth + Postgres).
# Without these, Strokeform stays local-first (browser localStorage only).
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
